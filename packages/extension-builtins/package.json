{
  "name": "@piarium/extension-builtins",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "description": "Browser-safe manifests for Piarium's built-in integration extensions.",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./host": {
      "types": "./dist/host.d.ts",
      "import": "./dist/host.js"
    }
  },
  "scripts": {
    "build:dependencies": "bun run --cwd ../extension-contract build && bun run --cwd ../extension-sdk build",
    "build": "bun run build:dependencies && tsc -p tsconfig.build.json && node scripts/build-host-builtins.mjs",
    "lint": "eslint \"./src/**/*.ts\" --config ../../eslint.config.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@piarium/extension-contract": "workspace:*"
  },
  "devDependencies": {
    "@piarium/extension-sdk": "workspace:*",
    "esbuild": "^0.28.1",
    "typescript": "~5.9.3",
    "typescript-language-server": "^5.3.0"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
