---
title: トラブルシューティング
description: よくあるセットアップとランタイムの問題と、すばやい対処です。
---

# トラブルシューティング

うまくいかないときは、下の症状を見つけて対処を試してください。

## Piarium コマンドが終了する、または起動しない

- Node.js `>=22.19` を確認する（開発でサポートされる基準は Node.js 24）
- ソースチェックアウトでは `bun run dev` または `bun run start:web` を実行する
- 公開済み CLI がある場合は `piarium --version` を実行し、必要なら再インストールする

## Web UI に到達できない

- ソースチェックアウトでは、`bun run dev` を起動したターミナルを読む
- 公開済み CLI がある場合は `piarium logs` でサーバーログを確認する
- 使用中のポートを確認する（開発は Vite URL、サーバーのデフォルトは `3000`）
- トンネルリンクを試す前に、まず `http://localhost:3000` を直接開く

## リモート / トンネルリンクが動かない

- CLI がインストールされているときは `piarium tunnel status --all` を実行する
- 同じインスタンス / ポートからトンネルを再起動する
- 前のトークンがすでに使われていたら、接続リンクを再生成する

セットアップ全体は [トンネル](/ja/tunnels/) を、リバースプロキシを使っている場合は [リバースプロキシ](/ja/reverse-proxy/) を参照してください。

## VS Code 拡張が接続しない

- Piarium サーバーが実行中であることを確認する
- このリポジトリから拡張を再ビルドまたは更新する
- VS Code ウィンドウを再読み込みして、接続を再試行する

## 関連

- [クイックスタート](/ja/quickstart/)
- [トンネル](/ja/tunnels/)
- [リバースプロキシ](/ja/reverse-proxy/)
