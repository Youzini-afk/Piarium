---
title: Túneis
description: Exponha o Varin com segurança para acesso remoto e móvel.
---

# Túneis

Um túnel é um link público para o seu Varin, para que você possa acessá-lo de um navegador comum em outra rede. Use `varin tunnel` para criar um para uma instância em execução.

> Conectar os **seus próprios dispositivos** (o app móvel, outro desktop) geralmente não precisa de túnel — [pareie-os](/pt-br/connect-devices/) e deixe o [Private Relay](/pt-br/private-relay/) criptografado de ponta a ponta cuidar do acesso fora de casa, sem nenhuma configuração.

## Pré-requisitos

O Varin inicia o CLI do provedor de túnel na sua máquina. Instale primeiro o provedor que você quer usar:

```bash
brew install cloudflared
brew install ngrok
```

Os túneis rápidos da Cloudflare funcionam com `cloudflared`. O Ngrok exige uma conta ngrok e um authtoken do painel do ngrok:

```bash
ngrok config add-authtoken <your-ngrok-token>
```

## Início rápido

1. Inicie o Varin:

```bash
varin
```

Se você pular esta etapa, `varin tunnel start` pode iniciar automaticamente um servidor CLI. Ao iniciar automaticamente, você pode passar opções do servidor como `--port`, `--host`, `--lan`, `--ui-password` e `--api-only`.

2. Inicie um túnel da Cloudflare:

```bash
varin tunnel start --provider cloudflare --mode quick
```

Ou inicie um túnel do Ngrok:

```bash
varin tunnel start --provider ngrok --mode quick
```

3. Verifique o status:

```bash
varin tunnel status
```

Quando o túnel estiver no ar, o `status` mostra uma URL pública. Abra-a, ou escaneie o QR code, para acessar o Varin de qualquer lugar.

Por padrão, o Varin imprime um QR code em sessões TTY interativas. Use `--qr` para forçar a saída do QR, ou `--no-qr` para desativá-la.

## Provedores

- `cloudflare`: modos quick, managed remote e managed local
- `ngrok`: modo quick

## Modos gerenciados

### Gerenciado remoto

Use um token + nome de host gerenciados pela Cloudflare:

```bash
varin tunnel start --provider cloudflare --mode managed-remote --token-file ~/.secrets/cf-token --hostname app.example.com
```

### Gerenciado local

Use uma configuração local do `cloudflared`:

```bash
varin tunnel start --provider cloudflare --mode managed-local --config ~/.cloudflared/config.yml
```

## Perfis (managed-remote)

Salve um perfil reutilizável:

```bash
varin tunnel profile add --provider cloudflare --mode managed-remote --name prod-main --hostname app.example.com --token-file ~/.secrets/cf-token
```

Inicie usando o perfil salvo:

```bash
varin tunnel start --profile prod-main
```

## Comandos úteis

```bash
varin tunnel providers
varin tunnel ready --provider cloudflare
varin tunnel ready --provider ngrok
varin tunnel doctor --provider cloudflare
varin tunnel doctor --provider ngrok
varin tunnel stop --port 3000
```

## Notas de comportamento

- um único túnel ativo por instância do Varin (porta)
- iniciar um novo modo/provedor na mesma instância substitui o túnel anterior
- gerar um novo link de conexão revoga o anterior não utilizado
- o auto-start do túnel preserva flags do servidor como `--ui-password` e `--api-only` nas configurações da instância usadas por reinícios e atualizações

## Relacionado

- [Conectar um Dispositivo](/pt-br/connect-devices/) — pareie seus próprios dispositivos sem uma URL pública
- [Segurança](/pt-br/security/) — proteja a interface antes de expô-la
- [Túneis no desktop](/pt-br/desktop-tunnels/) — configuração de túnel no app desktop sem iniciar pelo CLI
- [PWA e Acesso Móvel](/pt-br/mobile/) — acesse o Varin pelo celular
- [Solução de problemas](/pt-br/troubleshooting/) — se o link do túnel não funcionar
